# CI runs on every Pull Request and on pushes to main.
#
# Two jobs:
#   - backend:  Java 21 + Maven (build, test, mutation coverage via PIT)
#   - frontend: Node 22 + Angular (install, lint, build/type-check, test)
#
# NOTE: This assumes the repo has `backend/` and `frontend/` directories
# (i.e. a `pom.xml` in backend/, a `package.json` in frontend/).
# Rename the working-directory values below to match your real folders.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend — Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('backend/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Make mvnw executable
        run: chmod +x mvnw

      - name: Build
        run: ./mvnw clean package -DskipTests -q

      - name: Test
        run: ./mvnw test

      - name: Mutation coverage
        run: ./mvnw test-compile org.pitest:pitest-maven:mutationCoverage

      - name: Upload mutation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pit-mutation-report
          path: backend/target/pit-reports/

  frontend:
    name: Frontend — Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        # Lockfile-free install. The committed package-lock.json was generated on
        # Windows, so it only carries Windows rollup native-binary package entries
        # (npm/cli#4828); `npm ci` on Linux cannot install
        # @rollup/rollup-linux-x64-gnu and the Angular production build fails.
        # Removing the lockfile lets npm resolve the correct platform optional.
        # This mirrors the frontend Dockerfile. Windows dev keeps using the
        # committed lockfile directly (where it works).
        run: rm -f package-lock.json && npm install --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Build (type-check + AOT compile)
        run: npm run build -- --configuration production

      - name: Test
        run: npm test

  e2e:
    name: E2E — Playwright smoke
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: e2e/package-lock.json

      - name: Install e2e dependencies
        working-directory: e2e
        run: npm install --no-audit --no-fund && npm run install:browsers

      - name: Start Docker Compose stack
        run: docker compose up -d --build

      - name: Wait for stack health
        run: |
          for i in {1..30}; do
            if curl -sf http://localhost:8080/api/v1/health && curl -sf http://localhost:4200; then
              echo "Stack ready"
              exit 0
            fi
            echo "Waiting for stack... ($i)"
            sleep 5
          done
          echo "Stack did not become healthy"
          docker compose logs
          exit 1

      - name: Run Playwright tests
        working-directory: e2e
        run: npm test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: e2e/playwright-report/