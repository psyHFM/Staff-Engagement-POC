{
  "name": "staff-engagement-e2e",
  "version": "0.0.1",
  "private": true,
  "description": "Playwright smoke / vertical-slice acceptance tests for the Staff Engagement POC Docker Compose stack.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
